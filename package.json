{
  "workspaces": [
    "backend/*",
    "frontend/*"
  ],
  "scripts": {
    "dev": "lerna run dev",
    "new-version": "lerna version --conventional-commits --yes",
    "diff": "lerna diff",
    "install": "lerna run install"
  },
  "name": "blueprint",
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
