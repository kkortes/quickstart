{
  "name": "mongodb",
  "version": "1.0.2",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "./scripts/mongo",
    "stop": "kill -2 `pgrep mongo`;",
    "link": "exec ln -sfn ../../universal ./universal & exec ln -sfn ../../config ./config"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mongodb": "^3.5.7"
  }
}
