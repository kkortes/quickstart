{
  "name": "mongodb",
  "version": "1.0.2",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "./scripts/mongo",
    "stop": "kill -2 `pgrep mongo`;",
    "link": "(exec rm -Rf universal) && (exec ln -s ../../universal ./universal) && (exec rm -Rf config) && (exec ln -s ../../config ./config)",
    "eject": "(exec rm -Rf universal) && (exec cp -r ../../universal/ ./universal) && (exec rm -Rf config) && (exec cp -r ../../config/ ./config)"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mongodb": "^3.5.7"
  }
}
